# This workflow's AI orchestration is handled by VS Code Copilot Chat agents.
# See .github/agents/ for agent definitions.
# Entry point: @issue-agent implement issue #<N> in Copilot Chat.
name: developer-agent (disabled — use VS Code Copilot Chat)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'AI orchestration is handled by VS Code Copilot Chat agents. See .github/agents/'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use VS Code Copilot Chat. Entry point @issue-agent implement issue #N"

# Product

DarkFactory Weather is a prototype full-stack weather forecast application.

- The backend exposes a REST API that returns a 5-day weather forecast for a given climate region (tropical, arid, temperate, continental, polar).
- The frontend is a React SPA that lets users select a region and view the forecast in a table.
- The project is a prototype / learning scaffold — not a production service.

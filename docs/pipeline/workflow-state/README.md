# Workflow State

One JSON file per issue, named `<issue-number>.json`.
Created by the issue-agent when a plan is produced.
Updated by each agent as the pipeline advances.

See `docs/pipeline/shared-gates.md` — Workflow State Protocol for the schema.
